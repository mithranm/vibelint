{
  "PreToolUse": {
    "Edit": {
      "script": "python -c \"import sys; from pathlib import Path; file_path = sys.argv[1]; vibelint_path = Path('src/vibelint'); exit(0 if not str(file_path).startswith(str(vibelint_path)) else 1)\" \"$file_path\" || python -m vibelint.validators.self_validation \"$file_path\"",
      "description": "Run vibelint self-validation on vibelint source files before editing"
    },
    "MultiEdit": {
      "script": "python -c \"import sys; from pathlib import Path; file_path = sys.argv[1]; vibelint_path = Path('src/vibelint'); exit(0 if not str(file_path).startswith(str(vibelint_path)) else 1)\" \"$file_path\" || python -m vibelint.validators.self_validation \"$file_path\"",
      "description": "Run vibelint self-validation on vibelint source files before multi-editing"
    },
    "Write": {
      "script": "python -c \"import sys; from pathlib import Path; file_path = sys.argv[1]; vibelint_path = Path('src/vibelint'); exit(0 if not str(file_path).startswith(str(vibelint_path)) else 1)\" \"$file_path\" || python -m vibelint.validators.self_validation \"$file_path\"",
      "description": "Run vibelint self-validation on vibelint source files before writing"
    }
  }
}